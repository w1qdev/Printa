export type CreateProductParamsTypes = {
  name: string;
  description: string;
  price: number;
};

export type UpdateProductParamsTypes = {
  id: string;
  name: string;
  description: string;
  price: number;
};
